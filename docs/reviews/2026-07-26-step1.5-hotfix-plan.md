# Step 1.5 — Hotfix plan: stop new drift (awaiting restore passed)

**Gate:** Hetzner Console SSH fingerprint OK → `known_hosts` update → restore passed → code this → STOP → Step 2 (phase 2a reclass + 2b primary events).

## Scope (surgical)

1. **Hotel cancel:** remove `catch {}` around `reversePartnerEarning`; keep `postRefundCompensation` + reverse in the same Serializable tx already used by `cancelWithPolicy`; on failure rollback + Sentry (`bookingId`) + throw.
2. **Homestay/guide cancel:** post ledger + PartnerEarning (or reverse) on same path; unresolved partner → `UNATTRIBUTED` account + recon issue log — never skip money lines.
3. **Payment success:** stop hardcoded `partnerUserId: null` in `completeSuccessfulPaymentTx` (~L213) **and** Payme `performTransaction`; derive from hotel/listing/guide; fail if missing. Do **not** repair historical rows here.
4. **Not in scope:** PartnerEarning float → tiyin (Step 2).

## Tests

- Cancel failure mid-reverse rolls back ledger compensation.
- Homestay/guide cancel creates ledger (+ earning) entries.
- Payment success never calls ledger with null partner when partner exists; unresolved partner throws.

## Step 2 implication

Backfill from primary events (`HotelBooking`, `Payment`/`PaymentTransaction`, refund metadata), dry-run diff vs current PartnerEarning + ledger — not “each PartnerEarning → balanced tx”.
