/**
 * UI constants for the Feedback & Support panel.
 *
 * This file used to carry demo KPIs, charts and reviews that pages rendered when
 * their API failed or was never wired up. Every one of those is gone: support
 * pages now read FeedbackTicket data and show an error or empty state instead of
 * inventing numbers. Only genuine UI copy belongs here.
 */

/** Canned reply chips in the feed composer — text the agent inserts, not data. */
export const QUICK_REPLIES = ["Rahmat", "Tashakkur", "Kutamiz"];
